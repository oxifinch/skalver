<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
        <title><%= doc.title %> | Skalver</title>
		<meta name="description" content="Dashboard of Skalver web app"/>
		<meta name="viewport" content="width=device-width, initial-scale=1"/>
        <style>
            .hidden {
                display: none;
            }
            #reader_page_markdown {
                white-space: pre-wrap;
            }
        </style>
	</head>

	<body>
        <header>
            <button><a href="/dashboard">Go Back</a></button>
        </header>
        <main>
            <button id="reader_edit">Edit</button>
            <button id="reader_save">Save Changes</button>
            <div id="reader_page" style="width: 60rem; margin: auto; border: 2px solid black; padding: 1rem;">
                <div id="reader_page_content">
                    <%- doc.output %>
                </div>
                <div id="reader_page_markdown" class="hidden">
                    <pre contenteditable="true">
                        <%- doc.markdown %>
                    </pre>
                </div>
            </div>
            
            <a href="<%= doc.previous %>">Prev</a>
            <a href="<%= doc.next%>">Next</a>
        </main>
        <script>
            let currentDocumentFile = <%- JSON.stringify(doc) %>
        </script>
        <script src="/js/app.js"></script>
	</body>
</html>

