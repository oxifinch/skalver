<div class="library_section">
    <h2><%= section.name %></h2>
    <div class="library_section_books">
        <% section.books.forEach((book) => { %> 
            <h4><%= book.title %></h4>
        <% }); %> 
    </div>  
</div>   
